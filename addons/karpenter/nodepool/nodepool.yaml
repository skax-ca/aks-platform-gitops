apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  # ⚠️ 이 클러스터에서 이것이 **유일한 NodePool** 이고, NAP 은 NodePool 이 하나도 없으면 노드를
  #    만들지 못한다. aks-cluster 모듈이 node_provisioning_profile.default_node_pools 를 "None" 으로
  #    고정해 Azure 가 만들던 "default"·"system-surge" 두 NodePool 이 생기지 않기 때문이다 —
  #    GitOps 가 NodePool 을 전량 소유하게 하려는 결정이다.
  #    ⇒ 이 addon 은 cluster Secret 의 addon-karpenter 라벨로 옵트인하므로, NAP 을 켠 클러스터에
  #      그 라벨을 빠뜨리면 NodePool 이 0개가 되고 아무 노드도 뜨지 않는다.
  # 이름은 업스트림 공식 예제(general-purpose.yaml)에서 가져왔다.
  name: general-purpose
  annotations:
    kubernetes.io/description: >-
      플랫폼 baseline NodePool(범용). 업스트림 공식 예제(github.com/Azure/karpenter-provider-azure
      examples/v1/general-purpose.yaml)를 이 클러스터(cni_mode=overlay + network_data_plane=
      cilium, live/hub/aks/main.tf)에 맞춰 그대로 채택 — Cilium daemonset이 붙기 전 워크로드
      스케줄을 막는 라벨+startupTaint가 이 조합에서는 필수다.
spec:
  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 30s
  template:
    metadata:
      labels:
        # Karpenter가 Cilium daemonset 오버헤드를 예측하려면 필요하다(공식 예제 주석 원문:
        # "required for Karpenter to predict overhead from cilium DaemonSet").
        kubernetes.azure.com/ebpf-dataplane: cilium
    spec:
      nodeClassRef:
        group: karpenter.azure.com
        kind: AKSNodeClass
        name: general-purpose
      startupTaints:
        # Cilium이 아직 안 붙은 노드에 워크로드가 스케줄되는 것을 막는다
        # (karpenter.sh 공식 문서: Cilium startup taint 패턴, 업스트림 예제와 동일).
        - key: node.cilium.io/agent-not-ready
          effect: NoExecute
          value: "true"
      expireAfter: Never
      requirements:
        - key: kubernetes.io/arch
          operator: In
          values: ["amd64"]
        - key: kubernetes.io/os
          operator: In
          values: ["linux"]
        - key: karpenter.sh/capacity-type
          operator: In
          values: ["on-demand"]
        - key: karpenter.azure.com/sku-family
          operator: In
          values: ["D"]
