# hub 클러스터 전용 addon 파라미터 — cluster Secret 라벨에 담을 수 없는 값의 대피소
#
# 설계: aks-reference-infra의 .omc/plans/aks-platform-gitops-scaffold.md 1-1절이
#       예비해 둔 대안("안 되면 values.yaml 경로로 전환")을 실제로 적용한 결과.
#       cluster-secret.yaml의 albSubnetId 라벨 시도가 K8s 라벨 값 제약(63자·문자 제한)을
#       실측으로 위반함을 확인(2026-09-04) - 이 파일이 그 값의 실제 거처다.
#
# ⚠️ 이 파일은 k8s 매니페스트가 아니다 → bootstrap/root-app.yaml의 exclude 대상
#    (`clusters/**/values.yaml`, ** 라 depth 무관하게 이 경로도 포함).
#
# addons/baseline/alb-controller.yaml의 alb-loadbalancer ApplicationSet이 matrix
# generator(cluster + git files)로 이 파일을 읽어 subnetId 파라미터를 채운다.
albSubnetId: /subscriptions/57bb4b4a-6916-4c2d-9446-f908ba60e7d0/resourceGroups/rg-demo-hub-krc-workload-01/providers/Microsoft.Network/virtualNetworks/vnet-demo-hub-krc-main/subnets/snet-demo-hub-krc-alb
